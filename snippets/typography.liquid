{% comment %}theme-check-disable AssetPreload{% endcomment %}
{% comment %}
  Renders typography styles and preloads fonts.

  Usage:
    {% render 'typography' %}
{% endcomment %}

{% comment %} We check if its a system font, or a web-safe font {% endcomment %}
{% comment %}
  Renders typography variables and base mappings for new fonts.
  Make sure custom-fonts.liquid is included before </head>.
{% endcomment %}

{%- comment -%} Вимкни дублюючі font_face з settings, або лиши так: {%- endcomment -%}
{% if false %}
    <!-- old settings-based preload & font_face are disabled -->
  {% endif %}
  
  {% style %}
  :root{
    --font-heading: 'WT Monarch Nova', system-ui, -apple-system, Arial, sans-serif;
    --font-body: 'Neue Haas Grotesk Display Pro', system-ui, -apple-system, Arial, sans-serif;
  }
  
  html, body{ font-family: var(--font-body); }
  
  h1,h2,h3,h4,h5,h6,
  .h1,.h2,.h3,.h4,.h5,.h6{
    font-family: var(--font-heading);
    font-weight: 400; /* за UI-гідом — Roman/Regular */
  }
  {% endstyle %}
  